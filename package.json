{
  "name": "stella-face-auth",
  "version": "1.0.0",
  "description": "Face authentication system with Vercel deployment",
  "main": "index.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "echo 'Build completed'",
    "start": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "express": "^4.21.2",
    "cors": "^2.8.5",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "@vercel/node": "^3.0.0",
    "vercel": "^32.0.0",
    "nodemon": "^3.0.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
